name: Upload LinkedIn Client Kit to Repo

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  upload-ebook:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Download ebook from release
        run: |
          mkdir -p downloads
          gh release download linkedin-client-kit-v1 --repo shopshphereofficial-dev/digitalaikart --output "downloads/linkedin-client-kit.pdf"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit ebook to repo
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add downloads/linkedin-client-kit.pdf
          git commit -m "Add LinkedIn Client Kit PDF to repo" || echo "No changes to commit"
          git push
